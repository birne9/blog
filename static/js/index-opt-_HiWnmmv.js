const n=`## 为什么需要索引优化

第一篇讲过:索引就像给数据建了一份排好序的速查表,能把查询从"全表扫描"变成"直接定位"。但真实场景里,建了索引不代表查询就一定快:索引建错了列、查询写得让索引用不上、或者干脆少建了一个索引,都会让数据库退回全表扫描,数据量一大就慢得明显。所以除了会建索引,还要会"优化"索引:让每条高频查询都能命中正确的索引。

这一篇统一用 students 表做例子,它有三列常被查:class_id(班级)、name(姓名)、score(分数),主键是 id。

> 前端类比:索引优化就像前端性能优化里的"让缓存命中"。缓存建了但 key 对不上,照样每次全量重算;索引建了但查询条件不匹配,照样每次全表扫描。优化的第一步永远是先定位到底哪条查询慢。

## 最左前缀原则

联合索引是多列组成的索引,它内部先按第一列排序,第一列相同再按第二列排序,以此类推。这决定了它的使用规则:查询条件必须从最左列开始连续匹配,才能用上索引,这叫最左前缀原则。

先建一个班级加分数的联合索引:

\`\`\`sql
ALTER TABLE students ADD INDEX idx_class_score (class_id, score);
\`\`\`

下面四条查询,前两条能用上这个索引,后两条用不上:

\`\`\`sql
SELECT * FROM students WHERE class_id = 1;                    -- 用上
SELECT * FROM students WHERE class_id = 1 AND score > 80;     -- 用上
SELECT * FROM students WHERE score > 80;                      -- 用不上, 跳过了 class_id
SELECT * FROM students WHERE class_id > 1 AND score > 80;     -- 只有 class_id 这一段能用上
\`\`\`

第三条用不上的原因很直观:联合索引是先按 class_id 排序的,只给 score 条件时,排好序的结构帮不上忙,就像一本先按姓氏再按名字排序的电话簿,只告诉你想找名叫"伟"的人,没法跳着翻。第四条里 class_id > 1 是范围条件,范围之后索引结构就"断了",score > 80 只能靠二次过滤。

> 前端类比:联合索引就像一个按多级排序的数组:arr.sort((a, b) => a.classId - b.classId || a.score - b.score)。对这样的数组做二分查找,只能从第一级 classId 开始;只给 score 条件,数组的排序对你没有意义,只能线性扫。范围条件同理,classId 定位到一个区间后,score 在这个区间里并没有全局有序。

## 联合索引怎么设计

知道了最左前缀原则,设计联合索引就有的放矢了:把等值查询、区分度高的列放最左边,范围查询的列放最后。还是看 students,最常见的业务查询是"查某个班的成绩排名",条件是 class_id 等值加 score 范围,所以 idx_class_score (class_id, score) 就是正确设计;反过来建 (score, class_id) 就没法按班快速定位。

区分度是指列里不同值的多少:gender 只有两个值,区分度极低,给它单独建索引没什么用,查"所有男同学"几乎等于扫半张表;class_id 有几十上百个班,区分度高,值得建。

还有一条纪律:索引不是越多越好,一张表通常控制在三到五个以内。每个索引都要占磁盘空间,更关键的是每次插入、更新、删除数据,所有索引都要同步维护,写操作会变慢。所以只为高频查询的 WHERE 条件列建索引,查询很少用到的列别建。

> 前端类比:索引就像给数组维护多个排序副本,副本越多,push 一次要插入的地方越多。空间换时间是有账本的,读得多写得少的表多建几个划算,写多读少的表建多了就是负资产。

## 覆盖索引与回表

MySQL 的主键索引的叶子节点直接存整行数据,而普通索引(二级索引)的叶子节点只存索引列和主键值。当查询通过二级索引找到主键后,还要拿着主键再去主键索引里取完整的一行,这多出来的一步叫回表。

对比这两条查询:

\`\`\`sql
SELECT class_id, score FROM students WHERE class_id = 1;   -- 覆盖索引, 不用回表
SELECT * FROM students WHERE class_id = 1;                -- 需要回表取 name、gender
\`\`\`

第一条要查的列 class_id 和 score 都包含在索引 idx_class_score 里,直接在索引里就拿到结果了,这叫覆盖索引;第二条要整行数据,索引里没有 name 和 gender,必须回表。回表是随机读取,量大时开销可观,所以覆盖索引能明显提速,这也解释了为什么写查询时不要图省事 SELECT *,挑需要的列写。

> 前端类比:覆盖索引就像数据直接存在 Map 的 value 里,查到 key 就拿到全部;回表就像 Map 的 value 只存了个 id,还得拿 id 去另一个大结构里再查一次。少一次跳转,自然快一截。

## EXPLAIN 看执行计划

怎么知道一条查询到底用没用索引、扫了多少行?在查询前加 EXPLAIN 关键字,数据库不执行查询,只返回执行计划:

\`\`\`sql
EXPLAIN SELECT * FROM students WHERE class_id = 1;
\`\`\`

执行计划里有几个关键字段:type 是访问类型,从好到差大致是 const(主键等值)、ref(索引等值)、range(索引范围)、index(全索引扫描)、ALL(全表扫描),出现 ALL 就要警惕;key 是实际使用的索引,显示 NULL 说明没用到索引;rows 是预估要扫描的行数,越小越好;Extra 里的 Using index 表示走了覆盖索引,而 Using filesort 表示额外做了一次排序,通常是 ORDER BY 的列没被索引覆盖。

日常优化流程就是三步:发现慢查询(慢查询日志或监控),用 EXPLAIN 看它的 type、key、rows,然后决定是新建或调整索引,还是改写 SQL,改完再 EXPLAIN 确认一次,最后实测耗时对比。

> 前端类比:EXPLAIN 就是性能排查的测量工具,相当于前端用 Performance 面板和 console.time 定位慢点。优化的铁律是先测量再动手,不 EXPLAIN 就建索引,和不清分析就改代码一样,都是猜。

## 索引失效的常见场景

有些写法会让索引白白失效,写 SQL 时要避开。最典型的五个:

对列做运算或套函数,排序结构被破坏:

\`\`\`sql
SELECT * FROM students WHERE score + 10 > 90;   -- 失效, 要扫描每行算一遍
SELECT * FROM students WHERE score > 80;        -- 正确写法, 先移项
\`\`\`

前导通配符,LIKE '%明' 以通配符开头,索引无法定位起点;而 '小%' 这种前缀匹配可以正常走索引:

\`\`\`sql
SELECT * FROM students WHERE name LIKE '小%';   -- 能用索引
SELECT * FROM students WHERE name LIKE '%明';   -- 失效, 全表扫描
\`\`\`

隐式类型转换:假设表里学号 no 是 VARCHAR 类型,查询时传了数字,MySQL 会把整列都转成数字再比较,索引失效。正确做法是传字符串:

\`\`\`sql
SELECT * FROM students WHERE no = 20260001;     -- 失效, 列被隐式转换
SELECT * FROM students WHERE no = '20260001';   -- 正确
\`\`\`

OR 连接不同列:两个条件各走各的索引,数据库常常干脆全表扫描。拆成两条 UNION 查询或给两列分别建索引能解决:

\`\`\`sql
SELECT * FROM students WHERE class_id = 1 OR score = 90;   -- 可能全表扫描
SELECT * FROM students WHERE class_id = 1
UNION
SELECT * FROM students WHERE score = 90;                   -- 拆开各走索引
\`\`\`

否定条件:NOT、!=、<> 这类条件索引基本用不上,因为它们本质是"排除",而索引擅长的是"定位"。

> 前端类比:这些失效场景的共同点是破坏了"有序可定位"这个前提。对列做运算就像要求先对每个元素做 transform 再比较,只能线性扫;前导通配符就像在排好序的数组里找"以某后缀结尾"的元素,排序帮不上忙。记住本质,就不需要死记清单。

## 什么时候建索引

最后是时机问题。数据量小的表(几百几千行)全表扫描也很快,建索引反而增加维护成本,不用急着建。出现这些信号再考虑:表数据量大了(几万行以上)、某条查询明显变慢、WHERE 条件列比较固定、表读多写少。

建索引的动作本身也有成本:大表上直接建索引会锁表,生产环境通常用在线 DDL 或者在低峰期操作。建完一定要验证:EXPLAIN 确认 type 不再是 ALL,再实测查询耗时前后对比,确认收益了才算优化完成。

> 前端类比:建索引就像加缓存,不是所有数据都值得缓存——访问量大、变化少的数据才划算,而且缓存加完要验证命中率。索引和缓存一样,都是有维护成本的空间换时间,收益要用数据说话。

## 小结

索引优化就一条主线:让查询命中正确的索引,并让索引尽可能"少回表"。具体动作是:设计联合索引时遵循最左前缀(等值在前、范围在后),写查询时避开运算、前导通配、隐式转换、OR、否定这五类失效写法,用 EXPLAIN 验证每条高频查询的执行计划。按"定位慢查询 → EXPLAIN 分析 → 建索引或改写 SQL → 实测对比"这个流程走,就不会瞎优化。
`;export{n as default};
